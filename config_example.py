config = dict(
    username = '',              # Username for ghash.io
    key = '',                   # API key for ghash.io
    secret = '',                # Secret for ghash.io
    alert_addresses = [''],     # Email addresses to send alert emails too
    sender_address = '',        # Address to send emails from
    smtp_server = '',           # SMTP mail server to send mail through
    workers = ['']              # Names of workers on ghash.io. The format is username.workername
)
